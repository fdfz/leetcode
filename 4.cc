#include <iostream>
#include <set>
#include <vector>
using namespace std;
class Solution {
public:
    double findMedianSortedArrays(vector<int>& nums1, vector<int>& nums2) {
        double m1=nums1[nums1.size()/2],m2=nums2[nums2.size()/2];
        if(m1<m2)
        if(nums1[])
    }
};
int main()
{
    vector<int> v1={1,2};
    vector<int> v2={3,4};
    cout<<Solution().findMedianSortedArrays(v1,v2);
    return 0;
}