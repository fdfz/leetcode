#include <vector>
#include <iostream>
#include<cmath>
#include<algorithm>
using namespace std;
int main(){
    vector<string> v(128);
    string a="fd";
    cout<<a.contains(string{'f'});
    return 0;
}